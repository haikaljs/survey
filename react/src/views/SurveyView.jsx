import React from "react";
import PageComponents from "../components/PageComponents";

const SurveyView = () => {
    return (
        <PageComponents title="Create new survey">Survey form</PageComponents>
    );
};

export default SurveyView;
